import fs from 'node:fs';import {compileBCOG} from '../g9-p36-r4/compiler_bcog_r4.mjs';import {analyze,LAW} from './motif_r9.mjs';
const out=process.argv[2];const G=id=>({review_root:'r9',response_chunk_id:id,structural_role:'REVISION_COMMITMENT',block_type:'PROSE',attachment_key:id,antecedent_key:'Evaluation',local_scope_key:'SRP'});
const I=(id,paper,title,text)=>({id,paper,title,text,rebuttal_label:'SRP',perspective:'Evaluation',bcog:compileBCOG(text,G(id))});
const A={P1:{openreview_found:true,authorids:['~a1']},P2:{openreview_found:true,authorids:['~a2']},P3:{openreview_found:true,authorids:['~same']},P4:{openreview_found:true,authorids:['~same']},P5:{openreview_found:true,authorids:['~x']},P6:{openreview_found:true,authorids:['~y']},P7:{openreview_found:false,authorids:[]},P8:{openreview_found:true,authorids:['~z']}};
const independent=[I('a','P1','Alpha Study','We will add a detailed error analysis appendix.'),I('b','P2','Beta Study','We will include a detailed error analysis section.')];
const sharedAuthor=[I('c','P3','Gamma Study','We will add a detailed error analysis appendix.'),I('d','P4','Delta Study','We will include a detailed error analysis section.')];
const sharedMethod=[I('e','P5','FINOLA image method','We will add a FINOLA detailed error analysis appendix.'),I('f','P6','FINOLA image analysis','We will include a FINOLA detailed error analysis section.')];
const unknown=[I('g','P7','Unknown Study','We will add a detailed error analysis appendix.'),I('h','P8','Other Study','We will include a detailed error analysis section.')];
const z1=analyze(independent,A,'family'),z2=analyze(sharedAuthor,A,'family'),z3=analyze(sharedMethod,A,'family'),z4=analyze(unknown,A,'family'),ex=analyze(independent,A,'exact');
const validation={independent_positive:z1.stats.lineage_independent_primary_pairs===1,shared_author_rejected:z2.stats.lineage_independent_primary_pairs===0,shared_method_rejected:z3.stats.lineage_independent_primary_pairs===0,unknown_author_abstains:z4.stats.lineage_independent_primary_pairs===0,family_necessity_positive:z1.stats.lineage_independent_primary_pairs===1&&ex.stats.lineage_independent_primary_pairs===0};validation.pass=Object.values(validation).every(Boolean);
const result={law:LAW,independent:z1.stats,shared_author:z2.witnesses,shared_method:z3.witnesses,unknown:z4.witnesses,exact:ex.stats,validation};fs.writeFileSync(out,JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify(result,null,2));if(!validation.pass)process.exitCode=2;
